spring:
  datasource:
    url: jdbc:sqlite:file:./src/main/resources/db/marketplace.db  # Путь к SQLite
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: validate